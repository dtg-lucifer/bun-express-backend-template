/**
 * @deprecated Use `AuthController` from `./auth.controller` directly.
 * This shim is kept for backward compatibility during the OOP migration.
 */
export { AuthController as createAuthRouter } from "./auth.controller";
